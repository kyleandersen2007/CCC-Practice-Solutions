#include <bits/stdc++.h>

using namespace std;

int main()
{
    vector<int> numbers;

    int a, b, c, d;

    cin >> a >> b >> c >> d;
    
    numbers.push_back(a);
    numbers.push_back(b);
    numbers.push_back(c);
    numbers.push_back(d);

    int counter;

    for(int i = 0; i < numbers.size(); i++){
       
    }

}